export const APP_ROOT_ELEMENT = "#app";

export enum ROUTER {
  main = "/",
  login = "/login",
  register = "/register",
  notFound = "/404",
  profile = "/profile",
  profileUpdate = "/profile/update",
  password = "/password",
  chats = "/chats",
  error = "/error",
}
