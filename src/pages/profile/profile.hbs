{{#> Container }}
    <div class="profile-container">
    <h1 class="profile-title">My profile</h1>
        {{> Input label="Name" value="John" name="name" readOnly=true }}
        {{> Input label="Name" value="John Doe" name="name" readOnly=true }}
        {{> Input label="Email" value="john.doe@example.com" name="email" readOnly=true }}
        <div class="profile-form__actions">
            {{> Button label="Sign in" variant="primary" }}
            {{> Button label="Register" variant="secondary" }}
        </div>
    </div>
{{/ Container }}